//this file is completed. It loads App as the top level component 

import React from 'react';
import { render } from 'react-dom';
import App from './containers/App';

render (
  <App />,
  document.getElementById('root')
);
